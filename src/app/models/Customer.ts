export class Customer {
    constructor(public id: string, public name: string, public firstname: string, public age: string, public location: string, public gender: string, public telephone: string, public email: string) {

    }
}