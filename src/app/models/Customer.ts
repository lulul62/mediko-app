export class Customer {
    constructor(public name: string, public firstname: string, public age: string, public location: string, public gender: string, public telephone: string, public email: string) {

    }
}